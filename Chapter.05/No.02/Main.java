// 2. 次のプログラムの説明として、正しいものを選びなさい（１つ選択）。
public class Main {
    public static void main(String[] args) {
        int[] a;
        int b[];
        int[][] c;
        int d[][];
        int[] e[];
        int[][] f[];
    }
}

// A. 3行目でコンパイルエラーが発生する。
// B. 4行目でコンパイルエラーが発生する。
// C. 5行目でコンパイルエラーが発生する。
// D. 6行目でコンパイルエラーが発生する。
// E. 4行目と6行目でコンパイルエラーが発生する。
// F. 7行目と8行目でコンパイルエラーが発生する。
// G. すべての行でコンパイルエラーが発生しない。
// 正解は G. すべての行でコンパイルエラーが発生しない。